export default function Placeholder({ title }) {
  return (
    <main>
      <section className="lfdt-section">
        <h2>{title}</h2>
        <div className="lfdt-empty">In arrivo nella prossima fase della demo.</div>
      </section>
    </main>
  );
}
